import { POverweightPipe } from './p-overweight.pipe';

describe('POverweightPipe', () => {
  it('create an instance', () => {
    const pipe = new POverweightPipe();
    expect(pipe).toBeTruthy();
  });
});
