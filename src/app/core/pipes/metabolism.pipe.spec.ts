import { MetabolismPipe } from './metabolism.pipe';

describe('MetabolismPipe', () => {
  it('create an instance', () => {
    const pipe = new MetabolismPipe();
    expect(pipe).toBeTruthy();
  });
});
